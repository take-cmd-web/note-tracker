name: Daily Note Stats
on:
  schedule:
    # 毎日 日本時間 10:00 (UTC 01:00) に実行
    - cron: '0 1 * * *'
    # 毎日 日本時間 18:00 (UTC 09:00) に実行
    - cron: '0 9 * * *'
  workflow_dispatch:  # 手動実行も可能にする
jobs:
  track:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tracker
        env:
          NOTE_USERNAME: ${{ secrets.NOTE_USERNAME }}
          NOTE_COOKIE: ${{ secrets.NOTE_COOKIE }}
        run: python note_tracker.py
      - name: Commit results
        id: commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/
          if git diff --quiet && git diff --staged --quiet; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            git commit -m "Update stats $(date +'%Y-%m-%d %H:%M JST' --date='9 hours')"
            git push
            echo "changed=true" >> $GITHUB_OUTPUT
          fi
      - name: Purge Cloudflare Cache
        if: steps.commit.outputs.changed == 'true'
        env:
          CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
        run: |
          curl -X POST "https://api.cloudflare.com/client/v4/accounts/${CF_ACCOUNT_ID}/pages/projects/note-tracker/purge_cache" \
            -H "Authorization: Bearer ${CF_API_TOKEN}" \
            -H "Content-Type: application/json" \
            --data '{"purge_everything": true}'
      - name: Warn if no data change
        if: steps.commit.outputs.changed == 'false'
        run: |
          echo "::warning::CSVが更新されませんでした。NOTE_COOKIEが失効した可能性があります。Secretsを更新してください。"
          exit 1
